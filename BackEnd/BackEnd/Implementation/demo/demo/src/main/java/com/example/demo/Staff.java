package com.example.demo;

// note: this enumeration is not being utilised fully and will be implemented in the second version of our prototype (post-presentation).
// Staff class extends the User class. It represents a staff member.
public class Staff extends User{
    
    // Staff constructor to instantiate a staff by passing staffID to store within User class.
    public Staff(int staffID) {
        super(staffID);
    }
}
